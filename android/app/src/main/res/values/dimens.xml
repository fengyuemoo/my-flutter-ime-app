<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Keyboard key text -->
    <dimen name="ime_key_text_size">14sp</dimen>
    <dimen name="ime_key_text_size_small">12sp</dimen>

    <!-- Toolbar / composing -->
    <dimen name="ime_toolbar_button_text_size">18sp</dimen>
    <dimen name="ime_composing_text_size">15sp</dimen>

    <!-- Settings page -->
    <dimen name="ime_settings_title_text_size">22sp</dimen>
    <dimen name="ime_settings_label_text_size">15sp</dimen>
    <dimen name="ime_settings_button_text_size">15sp</dimen>

    <!-- Keyboard layout -->
    <dimen name="ime_row_height">46dp</dimen>
    <dimen name="ime_t9_main_height">180dp</dimen>
    <dimen name="ime_toolbar_height">46dp</dimen>

    <!-- New: keyboard body total height (main + bottom row) -->
    <dimen name="ime_keyboard_body_height">226dp</dimen>

    <!-- New: top bar height (toolbar/candidate strip container) -->
    <dimen name="ime_topbar_height">50dp</dimen>

    <!-- New: total IME view height = top bar + keyboard body -->
    <dimen name="ime_keyboard_total_height">276dp</dimen>
</resources>
